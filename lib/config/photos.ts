// 현장 사진 장수 상한 — 한 곳에서만 정한다.
//
// 왜 한 곳인가: 시공사례(포트폴리오) 글은 사장님이 처음부터 쓰는 게 아니라
// 작업 보고서의 전·후 사진을 그대로 받아서 만들어진다(lib/actions/portfolio.ts).
// 그래서 편집창 상한이 보고서 상한보다 작으면, 받아온 사진이 상한을 넘긴 채로
// 열려 "(8/5)"처럼 표시되고 추가 버튼이 잠긴다. 지울 수만 있고 되돌릴 수 없다.
// 두 화면이 같은 값을 보게 묶어두어야 이 어긋남이 다시 생기지 않는다.
export const REPORT_PHOTO_MAX = 10
