-- ============================================================
-- quote_tiers · quote_tier_services 제거
--
-- 왜 지우나: 플랜 이름·배수는 2026-08-19부터 코드 상수(DEFAULT_TIERS) 하나로 고정됐다.
-- 그 전에는 업체마다 quote_tiers 행을 두고 설정 화면(/dashboard/tiers)에서 고치게 했는데,
-- 정작 고객이 보는 견적서는 '기본/추천/프리미엄'을 코드에 박아 쓰고 있어서 무엇을 바꿔도
-- 화면이 안 바뀌었다. 설정 화면과 계산 경로를 걷어낸 뒤 테이블만 남아 있었다.
--
-- 지워도 잃는 게 없음을 확인하고 지운다(2026-08-21 실제 운영 DB 확인):
--   quote_tiers          96행 / 32개 업체 = 가입 시 자동 생성된 기본 3행씩.
--                        라벨 3종·배수 3종뿐 = 전 업체가 같은 기본값. 할인 설정 0건.
--   quote_tier_services  0행.
-- 즉 남은 건 아무도 손대지 않은 기본값이고, 코드에서는 이미 아무도 읽지 않는다.
-- ============================================================

-- 자식 테이블(quote_tiers를 참조)부터 지운다
drop table if exists public.quote_tier_services;
drop table if exists public.quote_tiers;
